/* A simple test harness for memory allocation. */

#include "mm_alloc.h"
#include <stdio.h>
int main(int argc, char **argv)
{
    /*
     * You can use this to test the program
     * available methods:
     * mm_malloc(), mm_free(), mm_realloc()
     */

    // Example: int* data = mm_malloc(sizeof(int * 4));
}
